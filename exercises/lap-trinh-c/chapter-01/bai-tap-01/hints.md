# Gợi ý

## Gợi ý 1
> Nhớ include thư viện `stdio.h` để dùng được hàm `printf`.

## Gợi ý 2
> Cú pháp cơ bản: `#include <stdio.h>` đặt ở đầu file.

## Gợi ý 3
> Đừng quên dấu chấm phẩy `;` ở cuối mỗi câu lệnh!
